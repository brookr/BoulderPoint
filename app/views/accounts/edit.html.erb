<%= render partial: "accounts/header", locals: { sub: "Change account settings" } %>

<%= simple_form_for @account, form_options do |f| %>
  <%= f.input :name %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
